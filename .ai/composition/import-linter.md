# Import Linter

Detected root package: `domoai`.

The generated .importlinter contains only a baseline acyclic_siblings
contract.

Use:

`project-composition-configure <project> claude|codex`

to inspect the actual architecture and add justified contracts such as:

- forbidden
- protected
- layers
- independence
- acyclic_siblings

Do not create architectural rules based only on folder names.
