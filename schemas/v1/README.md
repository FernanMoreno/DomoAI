# Schema v1

Generated JSON Schemas for the public DomoAI v1 contracts live here. The source of truth is the typed domain model under `src/domoai/domain/`; regenerate these files with `uv run python scripts/export_schemas.py`.
