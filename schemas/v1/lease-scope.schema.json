{
  "additionalProperties": false,
  "description": "The smallest authority scope that may have one physical owner.",
  "properties": {
    "tenant_id": {
      "maxLength": 200,
      "minLength": 1,
      "title": "Tenant Id",
      "type": "string"
    },
    "household_id": {
      "maxLength": 200,
      "minLength": 1,
      "title": "Household Id",
      "type": "string"
    },
    "deployment_id": {
      "maxLength": 200,
      "minLength": 1,
      "title": "Deployment Id",
      "type": "string"
    }
  },
  "required": [
    "tenant_id",
    "household_id",
    "deployment_id"
  ],
  "title": "LeaseScope",
  "type": "object"
}
