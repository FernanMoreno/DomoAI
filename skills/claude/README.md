# Claude host wrapper

This host wrapper installs or exposes the portable procedures from
`../core`. It does not fork their workflow, add tools, name a vendor adapter,
or bypass the single general MCP endpoint.
