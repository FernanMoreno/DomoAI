# Codex host wrapper

This host wrapper exposes the same portable procedures from `../core`. Host
presentation and installation may differ; semantic operations, approval order
and runtime authority do not. The wrapper uses the general MCP connection.
